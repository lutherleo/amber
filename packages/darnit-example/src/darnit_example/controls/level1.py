"""Python-defined controls for the Project Hygiene Standard.

All controls are now defined in TOML (example-hygiene.toml) with custom
sieve handlers registered in handlers.py. This file is kept empty for
backward compatibility with any code that imports it.
"""
