# example-store-plugin (feature 033 US3 fixture)

Third-party `AttestationStore` fixture. Exists so the pluggable-stores
suite can verify the `importlib.metadata.entry_points` discovery path
against a real installed distribution (not a monkeypatched one).

NOT for production use.
