"""Orion's standalone graph-build layer: repo -> Joern CPG -> Orion's own Neo4j.

Populated by Task A (schema.py, joern_adapter.py, persist.py). Kept as a subpackage so the
build layer stays isolated from the orchestration and harness code.
"""
